import { createAsyncThunk } from '@reduxjs/toolkit';
import { BURGER_CONSTRUCTOR_SLICE_NAME } from '../constants';

// export const fetchBurgerConstructor = createAsyncThunk(
//   `${BURGER_CONSTRUCTOR_SLICE_NAME}/fetchBurgerConstructor`,
//   async () => {
//     const data = await getBurg();
//         return data;
//   }
// )
